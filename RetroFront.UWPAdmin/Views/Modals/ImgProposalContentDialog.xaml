<ContentDialog
    x:Class="RetroFront.UWPAdmin.Views.Modals.ImgProposalContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RetroFront.UWPAdmin.Views.Modals"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    Title="TITLE"
    PrimaryButtonText="Annuler"
    SecondaryButtonText="Valider"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">

    <Grid Height="600" Width="600">
        <FlipView ItemsSource="{x:Bind ViewModel.IMGProposal,Mode=OneWay}" SelectedItem="{x:Bind ViewModel.Result,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
            <FlipView.ItemTemplate>
                <DataTemplate>
                    <controls:ImageEx VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Stretch="Uniform" Source="{Binding}"></controls:ImageEx>
                </DataTemplate>
            </FlipView.ItemTemplate>
        </FlipView>
    </Grid>
</ContentDialog>
